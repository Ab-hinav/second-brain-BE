# ---- Runtime ----
NODE_ENV=development
HOST=0.0.0.0
PORT=4000
LOG_LEVEL=info

# ---- CORS ----
# Comma-separated list; keep FE origin(s) here.
# CORS_ORIGIN=http://localhost:3000
# CORS_CREDENTIALS=true
CORS_ORIGIN=*

# ---- Database (PostgreSQL) ----
# Format: postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=disable
# For production, prefer ?sslmode=require
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/second_brain?sslmode=disable

# ---- Knex ----
KNEX_MIGRATIONS_DIR=./db/migrations
KNEX_SEEDS_DIR=./db/seeds

# ---- Auth.js (NextAuth) - FE forwards the raw session token (JWE) ----
# Must match NEXTAUTH_SECRET / AUTH_SECRET on the frontend. Used to decrypt the JWE.
NEXTAUTH_SECRET=__SET_ME__
NEXTAUTH_SALT=_cookie

# ---- Security / Limits ----
RATE_LIMIT_MAX=120
RATE_LIMIT_TIME_WINDOW=1 minute

# ---- Swagger ----
SWAGGER_ENABLED=true
SWAGGER_ROUTE=/docs
